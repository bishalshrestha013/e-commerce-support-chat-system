generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model Agent {
  id String @id @default(dbgenerated("gen_random_uuid()"))
  name String
  status Boolean
}